<h2 id="budget-logo">Budgét</h2>
<section id="card-container">
    <div class="table">
        <div class="container">
            <div class="add-title">
                <h1 class="add-text">VIEW PROFILE</h1>
            </div>
            <form class="add-form">
                <div class="pName">
                    <label>Name: <p>TL</p></label>
                </div>
                <div class="pName">
                    <label>Username: <p>test1</p></label>
                </div><br>

                <label>Remaining: <p>$5000</p></label><br>

            </form>

            <div id="chart_div"></div>

        </div>
    </div>
    <div class="wave wave1"></div>
    <div class="wave wave2"></div>
    <div class="wave wave3"></div>
    <div class="wave wave4"></div>
    <div class="wave wave1"></div>
    <div class="wave wave2"></div>
    <div class="wave wave3"></div>
    <div class="wave wave4"></div>
</section>
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script type="text/javascript">
    // Load the Visualization API and the corechart package.
    google.charts.load('current', {
        'packages': ['corechart']
    });

    // Set a callback to run when the Google Visualization API is loaded.
    google.charts.setOnLoadCallback(drawChart);

    // Callback that creates and populates a data table,
    // instantiates the pie chart, passes in the data and
    // draws it.
    function drawChart() {

        // Create the data table.
        var data = new google.visualization.DataTable();
        data.addColumn('string', 'Spending');
        data.addColumn('number', 10000);
        data.addRows([
            ['Transport', 500],
            ['Food', 1000],
            ['Entertainment', 500],
            ['Personal', 5000],
            ['Other', 2000]
        ]);

        // Set chart options
        var options = {
            'title': 'How Much $ I Spent Last Month',
            'width': 300,
            'height': 200
        };

        // Instantiate and draw our chart, passing in some options.
        var chart = new google.visualization.PieChart(document.getElementById('chart_div'));
        chart.draw(data, options);
    }
</script>